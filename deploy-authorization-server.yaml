apiVersion: apps/v1
kind: Deployment
metadata:
  name: authorization-server
  labels:
    k8s-app: authorization-server
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: authorization-server
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        k8s-app: authorization-server
    spec:
      containers:
      - image: wisskirchenj/authorization-server:0.1.0-SNAPSHOT
        name: authorization-server
        ports:
        - containerPort: 5050
          protocol: TCP
        envFrom:
        - configMapRef:
            name: authorization-server-cm
        env:
        - name: spring.datasource.password
          valueFrom:
            secretKeyRef:
              name: postgres-user-secret
              key: password
        resources: {}
        imagePullPolicy: IfNotPresent
      restartPolicy: Always
status: {}
